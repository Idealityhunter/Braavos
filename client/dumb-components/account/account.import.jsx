var account = React.createClass({
  render() {
    console.log(ActiveRoute.name('home'));
    return (
      <div>
        <p>
          hello account!!!
        </p>
      </div>

    );
  }
});

export const Account = account;
